ray
git+https://github.com/ray-project/ray_lightning#ray_lightning
scikit-learn
pathos
hyperopt
setuptools==59.5.0
ray[air]
CairoSVG
# Only necessary if running baselines
#spec2vec # Baseline
#ms2deepscore # Baseline
#matchms # Baseline
# Only on CalcUA cluster
google-api-core==2.8.0
googleapis-common-protos==1.56.0
protobuf==3.20.0
